import { ReformatDatePipe } from './reformat-date.pipe';

describe('ReformatDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ReformatDatePipe();
    expect(pipe).toBeTruthy();
  });
});
