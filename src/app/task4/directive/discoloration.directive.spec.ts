import { DiscolorationDirective } from './discoloration.directive';

describe('DiscolorationDirective', () => {
  it('should create an instance', () => {
    const directive = new DiscolorationDirective();
    expect(directive).toBeTruthy();
  });
});
